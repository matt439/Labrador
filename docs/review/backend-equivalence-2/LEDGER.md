# The 2026-08-19 audit, adjudicated item by item

> Read-only sweep by 350 agents, 2026-08-28, against the tree at `ca5b2e3`.
> Part of the [five-backend equivalence sweep](README.md). Nothing was built and
> nothing was run: every claim comes from reading source. **This document is the
> sweep as written and is not updated as findings are fixed** — see the README on
> why that disclaimer is load-bearing and what carries the live state instead.

The prior audit deliberately fixed nothing and said so. Nine days and twenty
commits later nobody could say which of its items were live — which is the
question this document answers, and the reason the sweep was run at all.

**106 items adjudicated** across the audit's three open documents and the
2026-08-26 survey's two render decisions: **35** landed, **34** not landed, **21** landed differently, **9** superseded, **7** moot.

`LANDED_DIFFERENTLY` is the interesting column and there are 21 of them: the
thing got built, but not in the shape the audit proposed, and in several cases the
substitute is better than the proposal. `SUPERSEDED` means this sweep's own
five-backend pass had already found and settled the item independently.

---


## TEST-GAP.md Vehicle A (A1–A5): tests/render/renderer_seam_tests.cpp

| Item | Status | What is true at `ca5b2e3` |
|---|---|---|
| **A1** CONTRACT: a view capacity below one is refused, and it is invalid_argument | NOT_LANDED | The vehicle landed; this case did not. `tests/render/renderer_seam_tests.cpp` is 97 lines and holds exactly ONE test case, at :51. Grep: `grep -n "create_device" tests/render/renderer_seam_tests.cpp` returns two hits and neither is a call — :22 `// WHAT BELONGS HERE is anything a Renderer answers before create_device` and :89 `CHECK(message.find("create_device") != std::string::npos);`. No `create_device(nullptr, 64, **At five backends:** Went from "passes on all three today; a lock" to passes on all five — the two new backends copied the guard verbatim, in the same position and with the same message (d3d12/renderer.cpp:1026, vulkan/renderer.cpp:781). No three-way  |
| **A2** CONTRACT: a marker is legal before there is a device | NOT_LANDED | No test, and the either/or the audit posed ("Either the seam states markers need a device or d3d11 gains three lines") was resolved NEITHER way. Grep: `grep -n "marker" tests/render/renderer_seam_tests.cpp` → empty. Tree-wide, `grep -rn "begin_marker|set_marker|end_marker"` finds no call in tests/ at all; the only caller anywhere is `engine/app/application.cpp:293` `this->renderer_->begin_marker(L"Render");` and :296 **At five backends:** The three-way split became five-way and got MORE lopsided, not less — which strengthens the case for picking the no-op answer. d3d11 is now the only one of five that touches anything. The other four are unconditional no-ops, each  |
| **A3** CONTRACT: loading a texture before there is a device is refused, by name | LANDED | It is the single test case in the file, and it is the reason the file exists. `tests/render/renderer_seam_tests.cpp:51` TEST_CASE("CONTRACT: a texture loaded before there is a device is refused, by name"). It builds a 2x2 r8g8b8a8_unorm TextureData by hand (:31-48, `two_by_two()`), declares the table before the renderer (:60-62), then asserts three things: the throw type at :73-75 `CHECK_THROWS_AS(add_texture_asset(r **At five backends:** The guard is now written five times, all named std::runtime_error throws at the top of add_texture_asset: d3d11/texture_factory.cpp:80-84 (`if (device_of(renderer) == nullptr)`), d3d12/texture_factory.cpp:110-114, gl/texture_facto |
| **A4** CONTRACT: window_size_changed before there is a device rebuilds nothing and says so | NOT_LANDED | Not landed, and the decision the audit said was needed first ("Needs a decision first") was never taken. Grep: `grep -n "window_size_changed" tests/render/renderer_seam_tests.cpp` → EMPTY. The d3d12 review already recorded the same negative twice: docs/review/d3d12/all-findings.md:538 "renderer_seam_tests.cpp never mentions window_size_changed", and :605-608 "grep across engine/render/null/, null_tests.cpp and render **At five backends:** The split went from 2-1 (gl+null true, d3d11 false) to 3-2, and the two new backends both joined the minority-turned-majority "false" side without anybody deciding that was the answer. FALSE before a device: d3d11 (renderer.cpp:68 |
| **A5** CONTRACT: a renderer with no device has no views | NOT_LANDED | Grep: `grep -n "view(|set_view_count|view_count" tests/render/renderer_seam_tests.cpp` → EMPTY. Not in the file, and not asserted anywhere else without a device first. The code answer is uniform across all five and needs no decision. `Impl::view_count = 0` by default — d3d11/backend.h:231, d3d12/backend.h:292, gl/backend.h:167, vulkan/backend.h:188, null/backend.h:112 — and `impl_->views` is empty until create_device **At five backends:** Five-way agreement, and there was never a split — this was the audit's other "a lock" case alongside A1. The two new backends inherited it structurally rather than by copying a check: `view_count = 0` and an empty `views` vector a |


## TEST-GAP.md Vehicle B — the harness prerequisite and B1..B7, against tests/render/pixel_tests.cpp at ca5b2e3

| Item | Status | What is true at `ca5b2e3` |
|---|---|---|
| **B0** Prerequisite: raise the harness capacity from 1 to 4 and give begin() a view count | LANDED | tests/render/pixel_tests.cpp:252-253 — `this->renderer_.create_device(this->window_, BUFFER_SIZE, BUFFER_SIZE, 4);` under the comment at :243-251 ("FOUR VIEWS OF CAPACITY, WHICH IS THE SAME NUMBER null_tests.cpp asks for ... This was 1 until the golden set made a multi-view frame checkable, and while it was 1 every multi-view path in both backends ... reached no rasteriser anywhere"). The view count reached begin() a **At five backends:** The mechanism is fine at five; the prose around it is not. :249 still reads "every multi-view path in both backends", written when two rasterisers existed and there are now four. The multi-view preamble at :1300-1314 names D3D11,  |
| **B1** read_back_buffer hands back exactly the back buffer, tightly packed | LANDED | TEST_CASE("CONTRACT: read_back_buffer hands back exactly back_buffer_size") at tests/render/pixel_tests.cpp:1499, whose own comment names the gap the audit found: "back_buffer_size is called by nothing else in this file and by nothing in tests/ at all, so until now the seam's 'exactly width * height * 4 bytes' was a sentence and not an assertion." The two assertions are at :1507-1512 — `CHECK(harness.buffer_size() == **At five backends:** This is the axis where five backends give three answers, and the test is written to survive that rather than to pick one. renderer.h:436-441 gives both Direct3D backends the size they were told and GL the window; Vulkan is a third |
| **B2** A viewport confines a sprite bigger than its pane | NOT_LANDED | `grep -n "set_viewport" tests/render/pixel_tests.cpp` gives seven call sites — :1235, :1271, :1276, :1353, :1407, :1442, :1539 — and every one still pairs with a destination exactly the pane's size, which is the audit's sentence unchanged. :1235 `set_viewport(Viewport(32.0f, 8.0f, 32.0f, 24.0f))` against `RectangleF(0.0f, 0.0f, 32.0f, 24.0f)`; :1271/:1276 the 31.5 split against dests of exactly 31 and 33 rows; :1353  **At five backends:** This has gone from a two-mechanism to a three-mechanism split and is the one B-item where the extra backends made the gap materially wider. D3D12 and Vulkan clip a pane with a scissor as well as the viewport: engine/render/d3d12/r |
| **B3** Views execute in view order, each under its own pane | LANDED | The ordering half is exact: TEST_CASE("CONTRACT: views draw in view order, whatever order they were filled") at tests/render/pixel_tests.cpp:1316, filled 1 then 0 and submitted 0 then 1 — "The two orders are opposite on purpose: everywhere else in this file they are the same order, so a backend that replayed in the order the calls arrived would pass every other case here and lose split-screen's layering entirely" (:1 **At five backends:** The seam prose has been amended for five and is accurate: renderer.h:406-424 now names all five submit shapes ("three of the five replay a vector, and the two that do not each execute a command list per view by a different route") |
| **B4** No view state survives a frame boundary | LANDED | TEST_CASE("CONTRACT: a view starts every frame holding nothing") at tests/render/pixel_tests.cpp:1401. Frame one sets a pane in the corner and a camera off the origin on view 0 (:1407-1408); frame two touches the same view index and sets neither, and the assertions at :1425-1427 are corner to corner — "A pane that survived would leave the left half black; a camera that survived would leave the far corner black, becau **At five backends:** The case's own comment at :1414-1417 is stale by two backends: "The three backends reset in three different places - begin_frame on two of them, set_view_count's rebind on the third - so this is the term that says the three arrive |
| **B5** A frame begun and never submitted contributes nothing to the next (defect B) | LANDED | TEST_CASE("CONTRACT: a frame that is never submitted contributes nothing") at tests/render/pixel_tests.cpp:1467, and it is built to the audit's own specification — B5 said it "must draw a sprite *and* text so the texture change forces a flush", and the case does exactly that at :1474-1481 under the comment "A SPRITE AND THEN TEXT, because the texture change between them is what forces a flush. Everything a draw recor **At five backends:** Both backends that landed after the case answer it in their own way and say so, so the term survived the two additions rather than being quietly re-broken. D3D12 closes a still-recording list because it cannot be reset otherwise ( |
| **B6** Submit is once per frame, and a second one adds nothing (said to FAIL on gl) | NOT_LANDED | `grep -n "submit" tests/render/pixel_tests.cpp` returns one call site — :495, inside Harness::read_frame — and `grep -n "submit" tests/render/null_tests.cpp` returns one, :104, inside Harness::end. Every other hit in both files is a comment or a TEST_CASE name. No case calls submit() twice, none calls end() twice on one frame, and renderer_seam_tests.cpp mentions submit nowhere (`grep -n "submit" tests/render/rendere **At five backends:** It has gone from the audit's 2-1 split to a 3-2 one, and the two halves are now split by mechanism rather than by accident. Adds nothing on a second call: D3D11, because submit skips any view whose `bound` flag is clear and View:: |
| **B7** A batch longer than one vertex buffer wraps and keeps its order (2050 sprites) | NOT_LANDED | `grep -rn "MAX_BATCH_SPRITES\|MAX_RUN_SPRITES\|MAX_PAGE_SPRITES\|2048\|2050" tests/` returns nothing at all (exit 1). Nothing in tests/ names a backend's batch capacity or comes near it. The largest loop in tests/render/pixel_tests.cpp is the 299-iteration texture re-load at :1805, which draws exactly once afterwards; the whole file's 41 draw_sprite calls are individually written, and null_tests.cpp's 19 likewise. Th **At five backends:** This is the item the two new backends changed most, and it is now a genuine four-way disagreement on what happens at sprite 2049 rather than the audit's implied one. D3D11 wraps one 256 KB buffer back to zero and asks for a DISCAR |


## TEST-GAP.md Vehicle B, cases B8-B14, plus the "if only seven land" closing line

| Item | Status | What is true at `ca5b2e3` |
|---|---|---|
| **B8** a texture change keeps call order, it does not group runs | NOT_LANDED | Grep is empty. The only place in tests/render/pixel_tests.cpp where a sprite and text share one DrawList is the ABANDONED frame at :1476-1481 — `abandoned.draw_sprite(...)` then `abandoned.draw_text(harness.font, L"AAAA", ...)` — and that list is deliberately never submitted ("AND NO end(), which is the whole case", :1483). Every other list in the file is single-texture: the full draw-call map of pixel_tests.cpp show **At five backends:** d3d12 and vulkan both make the case more expressive, not less. d3d12 records into a per-view ID3D12GraphicsCommandList in call order (submit at renderer.cpp:1224-1262 hands the closed lists to one ExecuteCommandLists in view order |
| **B9** a quarter turn turns the sprite clockwise on screen | LANDED | tests/render/pixel_tests.cpp:612 `TEST_CASE("CONTRACT: a positive rotation turns the sprite clockwise on screen")`. It uses `const float quarter_turn = 1.5707963f;` (:619) — literally the quarter turn B9 named — on a 16x4 bar pivoting on its own top-left corner (:626-628), and asserts both the direction (`CHECK(harness.at(30, 40) == RED); CHECK(harness.at(30, 46) == RED);` :634-635) and the two places an anticlockwis **At five backends:** The fill-rule half is where d3d12 and vulkan matter, and the golden set is what holds them. The case's own comment (:648-652) argues D3D11-versus-GL; at five backends d3d12 inherits D3D's top-left rule by specification and vulkan  |
| **B10** the filter reaches the sampler, and linear is not point | LANDED_DIFFERENTLY | HALF LANDED. `TextureFilter::linear` now reaches a real device, which it did not in August 2026 — tests/render/pixel_tests.cpp:703 `list.set_filter(TextureFilter::linear);` inside `SUBCASE("and under the linear filter")` (:700) of the B13 case. Before that, grep for the enumerator across tests/ hit only null_tests.cpp. THE SECOND HALF DID NOT LAND. Nothing asserts that linear differs from point. The two subcases make **At five backends:** Four sampler pairs now exist where two did, and all four spell the filter separately: d3d11 `D3D11_FILTER_MIN_MAG_MIP_POINT`/`_LINEAR` (renderer.cpp:528, :532), d3d12 `D3D12_FILTER_MIN_MAG_MIP_POINT`/`_LINEAR` into a 2-descriptor  |
| **B11** a source rectangle outside the texture clamps to the edge texel | NOT_LANDED | Grep for clamp/address terms in tests/render/pixel_tests.cpp returns five hits and none is an address mode: :52 (the do-not-pin list, about a glyph's left bearing), :342 (a comment about golden images), :1017-1018 (`DrawList wrapped` — a NEWLINE case, `L"A\nA"`), :1657 ("addressable"). No test anywhere passes a source RectangleI that exceeds its texture: pixel_tests uses `Harness::whole()` = RectangleI(0,0,2,2), `red **At five backends:** It went from a two-way agreement to a four-way one, all still uncontracted. d3d11/renderer.cpp:502-504 `AddressU/V/W = D3D11_TEXTURE_ADDRESS_CLAMP`; d3d12/renderer.cpp:825-827 `D3D12_TEXTURE_ADDRESS_MODE_CLAMP`; gl/renderer.cpp:52 |
| **B12** every format the seam names is uploaded, or refused by name | NOT_LANDED | The headline is unchanged: bc3_unorm has still never been on a device in this tree. Grep `TextureFormat::` across tests/ excluding the two file-reader test files returns exactly four hits, and all four are the same format: pixel_tests.cpp:178 (`flat_texture`), pixel_tests.cpp:207 (`two_level_texture`), renderer_seam_tests.cpp:44 (`two_by_two`), tests/scene/fanout_tests.cpp:75 — every one `TextureFormat::r8g8b8a8_unor **At five backends:** The five-way answer is a genuine split and d3d11 is the odd one out. b4g4r4a4_unorm: ACCEPTED by d3d11 (texture_factory.cpp:46-47, DXGI_FORMAT_B4G4R4A4_UNORM) and by d3d12's switch (:51-52) though d3d12 can still refuse it at Crea |
| **B13** a minified draw samples <level 0 | the chain> - blocked on a decision | LANDED | THE DECISION WAS MADE, IN THE SEAM, EXACTLY AS TEST-GAP PROPOSED. engine/render/renderer.h:159-172, beside `set_filter`: "IT CHOOSES BETWEEN TEXELS OF LEVEL ZERO AND NEVER BETWEEN LEVELS. A minified draw samples level zero however many levels the texture carries, under either filter. That is a term of this seam and not a backend's habit: the two answered it differently - one sampled the chain, the other did not - and **At five backends:** The decision was enforced on both new backends at the point they were written, not retrofitted, and each says so in its own file: d3d12/renderer.cpp:831-832 `MinLOD = 0.0f; MaxLOD = 0.0f;` under the comment at :819-823 "LEVEL ZERO |
| **B14** after a resize the back buffer is the new size, corner to corner | LANDED | Landed, and went well past what B14 proposed. THREE cases now cover it. (1) The assertion B14 named, verbatim in substance: tests/render/pixel_tests.cpp:1515 `SUBCASE("and it still does when the window changed and nobody said so")` inside `CONTRACT: read_back_buffer hands back exactly back_buffer_size` (:1499). It calls `harness.resize_window(BUFFER_SIZE, BUFFER_SIZE - 16);` (:1538), then `CHECK(harness.byte_count()  **At five backends:** This is the item where the five-backend answer genuinely changed the picture, and the seam prose has not caught up. engine/render/renderer.h:436-440 still enumerates three: "a swap chain does not follow its window, so both Direct3 |
| **SEVEN** the closing line - "If only seven land: A3, A2, B5+C1, B6+C3, B12, B3, B4+C2" | LANDED_DIFFERENTLY | FOUR OF THE SEVEN LANDED, ONE OF THOSE ONLY HALF; THREE DID NOT. Item by item. A3 (a texture loaded before there is a device is refused, by name) — LANDED, and as Vehicle A specified: a new file, added UNCONDITIONALLY. tests/render/renderer_seam_tests.cpp:51 `TEST_CASE("CONTRACT: a texture loaded before there is a device is refused, by name")`, wired in at tests/render/CMakeLists.txt:10 with no guard, so it runs in a **At five backends:** Two of the seven changed shape at five backends. A2: the split went from 3-way to 5-way and d3d11 is now alone. d3d12 (renderer.cpp:1363-1375), gl (renderer.cpp:876-890) and vulkan (renderer.cpp:1371-1385) are all three-no-op impl |


## TEST-GAP.md Vehicle C (five proposed null_tests cases + the release_device_resources grep) and the three structural claims about what CI cannot catch

| Item | Status | What is true at `ca5b2e3` |
|---|---|---|
| **C1** Vehicle C: null_tests case mirroring B5 — a frame begun and never submitted contributes nothing to the next | LANDED | tests/render/null_tests.cpp:435 `TEST_CASE("a frame that is never submitted contributes nothing to the next")`. It is the shape TEST-GAP specified: a sprite and then text, so the texture change forces a flush — :448-455 `DrawList abandoned = harness.begin(); abandoned.draw_sprite(...); abandoned.draw_text(harness.font, L"AA", ...)` — then no end(), then :459-462 `std::ignore = harness.begin(); const std::vector<Recor **At five backends:** The case runs only in x64-debug-null, but the term it pins is now enforced on all five: d3d11 drains via DrawList::View::discard (engine/render/d3d11/renderer.cpp:251-256), d3d12 closes the open list, gl/vulkan/null clear a vector |
| **C2** Vehicle C: null_tests case mirroring B4 — no view state survives a frame boundary | LANDED_DIFFERENTLY | Not in null_tests.cpp. Only one case in that file spans two frames at all — the C1 abandoned-frame case, whose two begin()s are at :448 and :459 — and it asserts emptiness, not carried-over state. The frame-boundary term landed on the device side instead: tests/render/pixel_tests.cpp:1401 `TEST_CASE("CONTRACT: a view starts every frame holding nothing")`, which sets a pane and a camera on view 0 in frame one (:1406-1 **At five backends:** pixel_tests.cpp:1414-1417 still says "The three backends reset in three different places - begin_frame on two of them, set_view_count's rebind on the third". At five that is a three-backend count: gl, vulkan and null all reset in  |
| **C3** Vehicle C: null_tests case mirroring B6 — submit is once per frame, and a second one adds nothing | NOT_LANDED | Grep is empty in both directions. `grep -n "submit()" tests/render/*.cpp` returns exactly three lines and none is a second call: tests/render/null_tests.cpp:104 (inside Harness::end), tests/render/pixel_tests.cpp:495 (inside Harness::read_frame), tests/render/pixel_tests.cpp:1732 (a comment). `grep -rn "second submit|submit twice|submitted twice" engine/ docs/ tests/` finds one hit in engine/: engine/render/vulkan/re **At five backends:** Went from three backends and two answers to five backends and three answers. Note the vulkan review already raised the comment at what was then renderer.cpp:1153 — docs/review/vulkan/all-findings.md:4809, "claims the restored layo |
| **C4** Vehicle C: the dropped-view case | LANDED | tests/render/null_tests.cpp:257 `SUBCASE("dropping a view that was drawn into is refused")`, inside `TEST_CASE("the seam's own rules hold here exactly as they do on a device")` at :247. It declares two views, draws into view 1 (:261-263), and asserts the refusal at :271-272 `CHECK_THROWS_AS(harness.renderer().set_view_count(1), std::logic_error);`. Its comment at :265-270 argues the term across backends by name. The  **At five backends:** The prior half's axis sweep returned dropped-views as AGREES across all five. The engine keeps it identically everywhere: d3d11 skips-but-finishes unbound views (engine/render/d3d11/renderer.cpp:914-920), d3d12 the same with `reco |
| **C5** Vehicle C: a released-handle/reload case | NOT_LANDED | No test releases anything. See item C6 for the grep. Nothing in tests/render/null_tests.cpp calls release_device_resources or reloads over a released handle; its load-path case at :282 `TEST_CASE("the load path is the real one, and its failures are the real ones")` covers only the two throws (a name the disk lacks, a name nobody loaded). What was built instead is the reload half without the release: tests/render/pixe **At five backends:** Five implementations of one function with no test between them: engine/render/d3d11/render_resources.cpp:83, engine/render/d3d12/render_resources.cpp:96, engine/render/gl/render_resources.cpp:82, engine/render/null/render_resource |
| **C6** TEST-GAP's grep: release_device_resources() is public seam API that nothing in tests/ calls on any backend — r | NOT_LANDED | Still exactly true, now across tests/ and bench/ both. Verbatim: $ grep -rn "release_device_resources" tests/ bench/ $ echo $? 1 Zero hits, exit 1. The full-tree grep returns eleven lines and not one is a test: engine/app/application.cpp:424 `this->render_resources_->release_device_resources();` is the only call site in the repository; the five definitions are engine/render/{d3d11,d3d12,gl,null,vulkan}/render_resourc **At five backends:** Two of the five hold a real GPU resource behind this call (d3d12's ID3D12Resource, vulkan's VkImage — renderer.h:362-372 and render_resources.h:112-131 both say so), and it is those two whose behaviour on the path is least like th |
| **S1** Structural claim 1: no backend-A-agrees-with-backend-B statement is expressible in-process — is that still tru | LANDED_DIFFERENTLY | The literal claim is STILL TRUE and the tree says so in its own words. tests/render/golden_image.h:23-25: "because a backend is chosen at configure time (T5), \"d3d11 agrees with gl\" is not a statement any one process can make." The class of drift the claim identified as uncatchable, however, is now caught — out of process, by exactly the mechanism TEST-GAP prescribed, in a modified shape. WHAT LANDED. tests/render/ **At five backends:** One set, four rasterising backends, five presets. The null backend is outside it by construction (RenderPixelTests is not built there). tests/render/golden_image.cpp:387-389 carries the measurement that set the tolerance — "this m |
| **S2** Structural claim 2: the harness is one view, one frame, one fixed window | LANDED | All three sub-terms are now false, and the prerequisite TEST-GAP named for Vehicle B ("raise the harness capacity from 1 to 4 and give begin() a view count") landed verbatim. - ONE VIEW: tests/render/pixel_tests.cpp:252-253 `this->renderer_.create_device(this->window_, BUFFER_SIZE, BUFFER_SIZE, 4);` The comment at :243-251 names the change and its cause: "FOUR VIEWS OF CAPACITY, WHICH IS THE SAME NUMBER null_tests.cp **At five backends:** The multi-view paths this unblocked are five-way, not three-way: d3d11's per-view deferred contexts, d3d12's array of closed lists in one ExecuteCommandLists, gl's and vulkan's per-view record-and-replay, null's gather. The harnes |
| **S3** Structural claim 3: the null recording is blind to the axes it would otherwise cover | LANDED_DIFFERENTLY | Half answered. Two axes were bought, the requested disclaimer landed but covers only one of them, and the texture half is unchanged. - WHAT THE RECORDING GAINED. engine/render/null/recording.h:80-87: `struct RecordedSprite { int view; TextureHandle texture; TextureFilter filter; Viewport viewport; SpriteVertex corners[4]; };` — filter and viewport were added by commit 126eded 'Assert the state a draw was recorded und **At five backends:** This is the claim where the five-backend count went most wrong in the file's own prose rather than in its code. tests/render/null_tests.cpp:31-33 — "That file needs a device: WARP on Direct3D, a real driver on OpenGL, and neither  |
| **D-A** Drift found while adjudicating S1: renderer_seam_tests.cpp still says the two device-side test files never hol | NOT_LANDED | tests/render/renderer_seam_tests.cpp:18-20: "RenderPixelTests needs an adapter and is not built against null; null_tests.cpp compiles only against null. Between them they never hold two backends to one statement." That is false at HEAD. RenderPixelTests holds four backends to one statement on every frame it reads: tests/render/pixel_tests.cpp:315 `void end()` calls :328 `golden::check_frame(...)`, and tests/render/go **At five backends:** Not a count error — a mechanism error. It is the mirror image of the counting drift the first half of this sweep found in renderer.h: there, prose that enumerates backends fell behind the backend list; here, prose that enumerates  |


## GAPS.md "Where the next pass should start", items 1-4, against the five-backend tree (HEAD ca5b2e3)

| Item | Status | What is true at `ca5b2e3` |
|---|---|---|
| **GAPS-1** The rasteriser fill rule against fractional edges (build_scaled_quad truncates nothing; D3D vs GL vs now Vulka | LANDED_DIFFERENTLY | THE PREMISE IS VERBATIM AT HEAD. engine/render/sprite_geometry.cpp:130-133 still truncates all four edges — `const float left = std::trunc(destination.left());` through `bottom`; build_scaled_quad at :139-152 computes `const Vector2F size(static_cast<float>(source.width) * scale, ...)` and calls build_quad with the raw `position`, truncating nothing. build_glyph_quad (:155-172) routes text through the scaled form. So **At five backends:** The axis went from a two-way specification split to a three-way one and the prose did not follow. At HEAD there are four rasterisers over three documents: d3d11 and d3d12 share the D3D rule (both mandate top-left exactly), GL 3.3  |
| **GAPS-2** Blend-result clamping, and GL never learning its framebuffer format — what do d3d12 and vulkan do? | LANDED_DIFFERENTLY | SPLIT ITEM. The half that was a backend divergence LANDED; the half that was about Colour did not, and turns out never to have been a divergence. THE GL HALF IS FIXED, by the same commit 6af3e61. engine/render/gl/renderer.cpp:334-357 now describes what ChoosePixelFormat actually handed back and refuses it per channel: PIXELFORMATDESCRIPTOR given = {}; if (DescribePixelFormat(this->device_context, format, sizeof(given **At five backends:** The item's divergence risk was entirely the GL negotiation, and adding two backends produced exactly one more negotiator, not three. At five backends the format question splits two-and-two: d3d11 and d3d12 DEMAND (DXGI_FORMAT_B8G8 |
| **GAPS-3** measure_text, can_render, first_unrenderable implemented three times inside the backend wall | LANDED | DISCHARGED, and verified against all five folders. `for b in d3d11 d3d12 gl vulkan null; do grep -n "measure_text\|first_unrenderable\|can_render" engine/render/$b/*.cpp engine/render/$b/*.h; done` returns nothing for any of the five (exit 1, no matches). The three methods now have exactly one definition each: engine/render/render_resources.cpp:77-81 `Vector2F RenderResources::measure_text(...) { return this->font(fo **At five backends:** This is the item where the five-backend count argues FOR the tree rather than against it. It was a three-way duplication when GAPS was written; it would be five-way now, of the two methods that carry a contract rather than a forwa |
| **GAPS-4** sprite_sheet.cpp destination_from written a second time and truncating where a glyph does not; is there a spri | NOT_LANDED | THE CODE IS UNTOUCHED, AT THE SAME LINES GAPS CITED. engine/render/sprite_sheet.cpp:19-24: RectangleF destination_from(const RectangleI& source, const Vector2F& position, float scale) { return { position, Vector2F(static_cast<float>(source.width) * scale, static_cast<float>(source.height) * scale) }; } It is still fed to the truncating path at :113-119 — `draw_list.draw_sprite(this->texture_, source, destination_from **At five backends:** This was never a per-backend axis and adding two backends did not make it one — destination_from is engine code above the seam, compiled once, and all five backends receive an already-truncated RectangleF. That is precisely why th |


## GAPS.md "Where the next pass should start", items 5-8, adjudicated against the five-backend tree at ca5b2e3

| Item | Status | What is true at `ca5b2e3` |
|---|---|---|
| **G5a** text_encoding.cpp includes <Windows.h> and sits in the unconditional source list, so it is compiled into the n | NOT_LANDED | Both halves verbatim at HEAD. `engine/render/text_encoding.cpp:3` is `#include <Windows.h>`, and the body is a single `MultiByteToWideChar(CP_UTF8, ...)` pair (`:16-25`). `engine/CMakeLists.txt:58` is `    render/text_encoding.cpp`, inside the unconditional `target_sources` list that ends at `:70` — the per-backend `if(LABRADOR_RENDER_BACKEND STREQUAL ...)` chain does not start until `:103`. GAPS cited `engine/CMakeL **At five backends:** Backend-independent — the file is in the unconditional source list, so all five presets compile it identically. The two backends added since the audit changed nothing here. |
| **G5b** widen() is the only producer of surrogate pairs while font.h walks per UTF-16 code unit with a bare static_cas | NOT_LANDED | THE CROSSING IS STILL NOT RUN. `grep -rn 'U0001|\\xD8[0-9A-Fa-f][0-9A-Fa-f]|0xD800' tests/render/` returns nothing (exit 1) — no astral literal reaches any render test. `grep -rn 'widen' tests/render/font_tests.cpp tests/render/render_resources_tests.cpp tests/render/pixel_tests.cpp` returns nothing (exit 1) — no test feeds widen()'s output into a glyph walk. `widen` appears in exactly five places in the whole tree:  **At five backends:** Backend-independent by construction. font.h's glyph walk, first_unrenderable and measure_text are engine code compiled once (CLAUDE.md, "Nothing a backend does decides where a pixel goes"), and render_resources.cpp's two resource  |
| **G6a** ViewportManager::fullscreen_viewport() sizes from resolution_manager_->resolution_vec(), not Renderer::back_bu | NOT_LANDED | Structurally unchanged, and deliberately so. `engine/render/viewport_manager.cpp:14-18`: `Viewport ViewportManager::fullscreen_viewport() const { Vector2F res = this->resolution_manager_->resolution_vec(); return { 0.0f, 0.0f, res.x, res.y }; }`. `player_viewport` (`:25-29`) and `viewport_dividers` (`:78`) read the same source. The class still holds no device and no Renderer — `engine/render/viewport_manager.h:58` is **At five backends:** The producer side is backend-blind — ViewportManager names no graphics type at all. The consumer side splits 4-1 at five backends: d3d11 (`renderer.cpp:939-944`, GetOutputSize), d3d12 (`:1265-1270`, output_size), vulkan (`:1202-12 |
| **G6b** The startup path is untested and fullscreen-at-startup is the candidate: real client area differing from the p | LANDED_DIFFERENTLY | THE MECHANISM HALF WAS ALREADY ANSWERED IN CODE FOUR DAYS BEFORE THE AUDIT'S BASE COMMIT, and the audit did not open the file. `engine/app/window.cpp:203-215` names this exact scenario: // AND THE WM_SIZE THIS PRODUCES IS WORTH SOMETHING. It arrives before // the device exists, so the renderer half of on_window_size_changed // still does nothing - DeviceResources::WindowSizeChanged returns early // with no window set **At five backends:** The startup ordering is in engine/app/ and is identical under all five presets — the shell hands a `void*` HWND to `create_device`. What differs is only what create_device is given: on gl the window's client area is authoritative  |
| **G6c** Check vulkan specifically, since it is the backend where the presentation engine reports a size change rather  | LANDED | REFUTED FOR VULKAN, AND THE BACKEND ARGUES IT AT LENGTH. `engine/render/vulkan/renderer.cpp:1202-1212`: Vector2F Renderer::back_buffer_size() const { // THE COLOUR TARGET'S, WHICH IS THE SIZE THE SHELL SAID. renderer.h // makes this the size of the buffer rather than of the last thing // anyone said, and on this backend those two are the same number for a // reason no other backend has: the frame is not drawn into a  **At five backends:** This is the item where five backends changed the shape of the answer. At three backends the split was d3d11-and-null against gl. At five it is 4-1 with the two new backends both on the majority side, and the new one that was expec |
| **G7a** draw_object.cpp set_draw_rotation_by_rectangle_rotated is a setter with an empty body and a TODO, silently dis | NOT_LANDED | Verbatim at HEAD, `engine/render/draw_object.cpp:68-71`: void DrawObject::set_draw_rotation_by_rectangle_rotated(const RectangleRotated& /*rect*/) { // TODO: Implement this function } Its silent consumer is also unchanged: `engine/render/visual.cpp:36` is `this->rectangle_ = rect_rotated.rectangle_rotated_to_axis();` and the constructor at `:24-37` never calls `set_draw_rotation`, so a Visual built from an oriented r **At five backends:** Engine-side, above the seam and identical in all five configurations. Note that it is only *observable* in one of them: asserting the rotation reached a quad needs a readable draw record, which is engine/render/null/ and therefore |
| **G7b** sprite_frame.h stores origin_ and rotated_ with no accessor for either, so an atlas frame packed rotated 90 de | LANDED | Both halves answered, in one commit, and the class no longer resembles the one GAPS described. `engine/render/sprite_frame.h:49` is `const mattmath::Vector2F& origin() const;` (defined `sprite_frame.cpp:27-30`), documented at `:40-49` as "unscaled source texels measured from this frame's top left - the seam's own units for an origin". `rotated_` is gone: `sprite_frame.h:51-52` is now the whole private section, `sourc **At five backends:** The behaviour is engine-side and identical everywhere, but its test is x64-debug-null only (tests/render/CMakeLists.txt:43-45 adds null_tests.cpp for that backend alone). Four of the five presets run the loader half and none of th |
| **G7c** rotation_origin.h has zero users anywhere | NOT_LANDED | The file is unchanged and the grep is still empty. `engine/render/rotation_origin.h` is twelve lines: `enum class RotationOrigin { center, top_left, left_center, no_rotation };`. `grep -rn '#include "engine/render/rotation_origin.h"' engine/ samples/ tests/` returns nothing (exit 1). `grep -rn 'rotation_origin' engine/ samples/ tests/ --include=*.h --include=*.cpp` returns nothing (exit 1) — not even the file matchin **At five backends:** None — a twelve-line header in engine/render/ that no backend includes and no build step compiles. |
| **G7d** Commit 169a3c0 "Answer the two sheet keys the loader read and dropped" - exactly what it did, and which of ite | LANDED | WHAT IT DID, from the commit and verified in the tree. Nine files, 427 insertions. It answered `docs/survey/2026-08-26.md` section 3.5, not GAPS item 7, and the two keys are decided in opposite directions: (1) `rotated` REFUSED BY NAME at load. `engine/assets/sprite_sheet_loader.cpp` throws naming the file, the position, the frame and the key; the argument is that honouring it "is not a branch - a turned frame draws  **At five backends:** The refusal is at load, above every backend. The composition it enabled is asserted on the null backend alone; the commit states it ran 13/13 on d3d11, d3d12, gl and vulkan and 12/12 on null, and that the golden set is untouched b |
| **G8a** Twelve .cpp under engine/render/ with neither an axis nor a test file - how many have a test now? | NOT_LANDED | TWO OF THE TWELVE GAINED REAL COVERAGE; TEN DID NOT; ZERO GAINED A TEST FILE OF THEIR OWN. The mechanical check — for each `engine/render/*.cpp`, does `tests/render/<name>_tests.cpp` exist — returns NOTEST for all twelve GAPS named: animation_object, animation_strip, draw_object, label, sprite_frame, sprite_sheet, sprite_sheet_object, text, text_drop_shadow, text_object, texture_object, visual. (It also returns NOTES **At five backends:** All twelve are above the seam and compiled identically in all five configurations. The asymmetry is in where they can be *observed*: anything that ends in a quad is readable only under x64-debug-null (engine/render/null/recording. |
| **G8b** Of the twelve, the three that carry pixel arithmetic reaching the seam: sprite_sheet destination_from, text_dr | LANDED_DIFFERENTLY | PARTIALLY CLOSED, AND THE PART THAT CLOSED IS NOT THE PART GAPS POINTED AT. (1) `engine/render/sprite_sheet.cpp:19-24` — `destination_from` is unchanged and still the size arithmetic written a second time: `return { position, Vector2F(static_cast<float>(source.width) * scale, static_cast<float>(source.height) * scale) };`. What changed is that SpriteSheet now HAS tests — but they exercise the wrong two of four overlo **At five backends:** All three are engine-side quad arithmetic, so the contract is one contract and all five backends consume its output. The truncation asymmetry in (1) is the only one of the three that could in principle differ at a rasteriser, and  |


## GAPS.md's 16 unverified high-or-medium candidates — RECONSTRUCTED (not recovered) at five backends

| Item | Status | What is true at `ca5b2e3` |
|---|---|---|
| **FS-1** factory-split (1 of 3): the seam prices a port's content duty at "thirty lines and nothing else" — it is 48-37 | SUPERSEDED | RECONSTRUCTION, NOT RECOVERY — GAPS.md:35-41 records only the count (3), never the candidates. engine/render/resource_factory.h:14-20 verbatim at HEAD: "a backend supplies add_texture_asset and / nothing else - which is thirty lines and the whole of what a port owes / for content." wc -l at ca5b2e3: engine/render/null/texture_factory.cpp 48, engine/render/d3d11/texture_factory.cpp 115, engine/render/gl/texture_factor **At five backends:** The old row was a three-way spread of 35-162; it is now a five-way spread of 48-378, and the 12.6x factor is measured against the largest. THE CURRENT SWEEP ALREADY FOUND AND KEPT THIS: harvest/axes.json factory-split candidate [0 |
| **FS-2** factory-split (2 of 3): what a half-built texture leaves behind when add_texture_asset throws — five backends, | LANDED | RECONSTRUCTED. The seam states one post-condition on failure and it is about the table, asserted at tests/render/renderer_seam_tests.cpp:92-96: "Nothing was added under that name either" / `CHECK_THROWS_AS(std::ignore = resources.resolve_texture("quad"), std::out_of_range);`. Nothing states what the BACKEND must give back. Measured at HEAD by `grep -c catch engine/render/*/texture_factory.cpp`: d3d11 1 (a comment at  **At five backends:** At three backends the axis had one throwing factory (gl) and two that could not fail this way, so "what is left behind" was invisible. At five it is a genuine spread: vulkan rolls back explicitly and documents why, d3d12 orders it |
| **FS-3** factory-split (3 of 3): add_texture_asset now stalls the GPU on two of five backends, the seam names no such c | LANDED_DIFFERENTLY | RECONSTRUCTED. engine/render/d3d12/texture_factory.cpp:272-273 — `impl.execute_frame_list(); impl.device_resources.wait_for_gpu();`. engine/render/vulkan/texture_factory.cpp:311 — `device_resources.end_upload(commands);`, whose comment at :309-310 reads "Ends, submits and waits, which is what the staging buffer's lifetime demands." d3d11 hands `D3D11_SUBRESOURCE_DATA` to the runtime (texture_factory.cpp:96-107, no wa **At five backends:** Three-way to five-way, and the direction reversed: at 2026-08-19 no backend waited on a GPU inside the factory, so the seam's silence cost nothing. Two of the five now do. THE COMMENT HALF IS ALREADY BANKED as harvest/axes.json fa |
| **SH-1** seam-hygiene (1 of 3): the folder wall's module is a hard-coded (render|audio) alternation, so engine/input/xi | SUPERSEDED | RECONSTRUCTION, NOT RECOVERY — GAPS.md:35-41 records only the count (3). cmake/check_engine_includes.cmake:97 selects with `REGEX "^[ \t]*#[ \t]*include[ \t]*[\"<](\\.\\./)*(engine/)?(render|audio)/[A-Za-z0-9_]+/[A-Za-z0-9_]+\\.h[\">]"` and :103 re-matches `"(render|audio)/([A-Za-z0-9_]+)/[A-Za-z0-9_]+\\.h"`. Only the BACKEND is captured; the module is listed. The file's own summary at :82-84 says the opposite: "The  **At five backends:** None of the five render backends diverge — all five sit inside the guarded render/ module and all five are clean in both directions (see SH-3's grep). The divergence is between the guard's stated coverage and its actual coverage,  |
| **SH-2** seam-hygiene (2 of 3): the wall scans engine/ only — tests/, bench/ and samples/ are outside it, and sample st | LANDED | RECONSTRUCTED. CMakeLists.txt:19-22 invokes the check with `"-DENGINE_DIR=${CMAKE_CURRENT_SOURCE_DIR}/engine"`, and cmake/check_engine_includes.cmake:23 is `file(GLOB_RECURSE engine_sources "${ENGINE_DIR}/*.h" "${ENGINE_DIR}/*.cpp")` — one root, engine/. The file's own justification names the damage as being outside that root: :50-52, "engine/app/application.h to include it, and every state file in both samples to co **At five backends:** Unchanged from three backends to five in mechanism, but the surface grew: there are now ten backend folders under engine/ (five render, two audio, one input, plus the two module roots) and the wall's blind side is the same one it  |
| **SH-3** seam-hygiene (3 of 3): four of the five backends link their graphics SDK PUBLIC and one links it PRIVATE, and  | LANDED | RECONSTRUCTED. engine/CMakeLists.txt:110 `target_link_libraries(LabradorEngine PUBLIC d3d11 dxgi dxguid)`; :138 `PUBLIC d3d12 dxgi dxguid`; :160 `PUBLIC opengl32`; :193-194 `find_package(Vulkan REQUIRED)` / `target_link_libraries(LabradorEngine PRIVATE Vulkan::Vulkan)`; :217-220 null links nothing. The reasoning exists for exactly one of them, at :184-192: "PRIVATE, WHICH IS THE ONE THING THE INCLUDE CHECK CANNOT SEE **At five backends:** A pure five-way split that did not exist at 2026-08-19, when both real backends were PUBLIC and it looked like a convention rather than a choice. Vulkan landing PRIVATE made it a choice, and the file wrote down the reason for one  |
| **EP-1** error-parity (1 of 2): d3d12 alone refuses a texture load once 256 distinct names exist, and the seam names no | SUPERSEDED | RECONSTRUCTION, NOT RECOVERY — GAPS.md:35-41 records only the count (2). engine/render/d3d12/backend.h:287 `static const int TEXTURE_CAPACITY = 256;`. engine/render/d3d12/renderer.cpp:474-486 verbatim at HEAD: `if (this->next_texture_slot >= TEXTURE_CAPACITY) { throw std::runtime_error("Texture '" + name + "' does not fit: this backend's descriptor heap holds " + std::to_string(TEXTURE_CAPACITY) + " distinct texture  **At five backends:** A refusal that did not exist at three backends: d3d11, gl, vulkan and null all store textures in an uncapped Registry, and vulkan explicitly grows descriptor pools on demand instead. THE CURRENT SWEEP ALREADY FOUND THIS TWICE, und |
| **EP-2** error-parity (2 of 2): renderer.h says "All five throw it" and exactly one configuration ever checks a seam th | SUPERSEDED | RECONSTRUCTED, and independently re-verified. engine/render/renderer.h:389-396 verbatim at HEAD: "AND std::logic_error FOR A COUNT LOWERED PAST A VIEW SOMETHING HAS / ALREADY DRAWN INTO ... All five throw / it, which is what makes it a term of the seam rather than one / backend's caution." `grep -n "CHECK_THROWS|REQUIRE_THROWS" tests/render/pixel_tests.cpp tests/render/null_tests.cpp` returns SIX hits and every one i **At five backends:** At 2026-08-19 the sentence said three and the evidence was the same: null_tests.cpp only. Adding two backends tripled the unchecked surface without changing the harness. I opened every file and the greps above are the whole basis. |
| **PL-1** pixel-logic-leak (1 of 2): same_viewport is hand-copied between the only two backends that have it and the two | SUPERSEDED | RECONSTRUCTION, NOT RECOVERY — GAPS.md:35-41 records only the count (2). engine/render/gl/renderer.cpp:35-39 compares four fields; engine/render/vulkan/renderer.cpp:40-46 compares six; neither calls `Viewport::operator==` (engine/render/viewport.cpp:78-86), which compares six. The two sit under a word-identical comment. The 2026-08-19 INVENTORY.md:40 verdict for this axis was "legitimate; duplication is the residual  **At five backends:** At three backends there was one copy of this helper, so no divergence was expressible. At five there are two copies and they already disagree, which is the axis's own prediction coming true one level below observability. THE CURRE |
| **PL-2** pixel-logic-leak (2 of 2): the four-way hand-copy is now caught by the golden image set — the residual is what | LANDED_DIFFERENTLY | RECONSTRUCTED. WHAT WAS BUILT INSTEAD: tests/render/golden/ holds 50 PNGs (`ls | wc -l` = 50) and tests/render/pixel_tests.cpp:56-63 states the claim — "Each case's read-back is compared byte for byte against a PNG of it ... which is the one statement here that a difference BETWEEN two backends can fail ... Fifty-three frames; fifty of them are 64x64 on every backend and identical across the four that rasterise." Tha **At five backends:** The three-way axis (6/6/1 backend-owned pixel decisions) is now 6/7/6/8/1 — d3d12 adds `to_scissor` because a D3D12 list starts with an empty scissor, vulkan adds the negative viewport height, the ≥0 scissor clamp and the empty-pa |
| **FM-1** format-mapping (1 of 1): d3d11 is now the only backend whose device refusal names neither the texture nor the  | SUPERSEDED | RECONSTRUCTION, NOT RECOVERY — GAPS.md:35-41 records only the count (1). The contract is engine/render/resource_factory.h:74-77: "Throws std::runtime_error naming `name` and the format if the device will not take it, which is the answer a backend that cannot upload block compression owes rather than a blank texture." engine/render/d3d11/texture_factory.cpp:106-107 is `ThrowIfFailed(device_of(renderer)->CreateTexture2 **At five backends:** At 2026-08-19 this was one of two backends failing the floor, which read as a Direct3D property. d3d12 fixed it deliberately and wrote down why, so the two Direct3D backends now disagree about a term the seam does state — a three- |
| **UM-1** upload-mechanics (1 of 1): only d3d12 can refuse a texture because the number of distinct texture NAMES ran ou | SUPERSEDED | RECONSTRUCTION, NOT RECOVERY — GAPS.md:35-41 records only the count (1). Same code as EP-1, on the upload path rather than the error path: engine/render/d3d12/texture_factory.cpp:293-296 — `const int existing = resources.impl()->descriptor_slot(name); const int slot = existing >= 0 ? existing : impl.allocate_texture_slot(name);` — into engine/render/d3d12/renderer.cpp:474-486. The mitigation is real and documented at **At five backends:** A per-name resource that only exists on one of the five: d3d11 binds an SRV at PS slot 0, gl a GLuint at unit 0, vulkan allocates a descriptor set per run from a pool that grows, null stores two ints. THE CURRENT SWEEP ALREADY FOU |
| **BB-1** batch-breaks (1 of 1): no test in any configuration has ever submitted a view that changes texture or filter m | SUPERSEDED | RECONSTRUCTED, and independently re-verified by counting. `grep -n "set_filter" tests/render/pixel_tests.cpp` returns exactly two hits, :690 and :703, and reading tests/render/pixel_tests.cpp:687-714 shows they are in SEPARATE SUBCASEs — `SUBCASE("under the point filter")` at :687 and `SUBCASE("and under the linear filter")` at :700 — each opening its own `harness.begin()` and closing with `harness.end()` around ONE  **At five backends:** The break key is the same five conditions on all four rasterisers, but d3d11 and d3d12 read the sampler from the view's CURRENT filter at flush (d3d11/renderer.cpp:163, d3d12:238) while gl and vulkan read the run's CAPTURED filter |
| **RZ-1** resize (1 of 1): restore-from-minimise still forwards no size, and the five backends now compensate in three d | LANDED | RECONSTRUCTED. THE 2026-08-19 ITEM IS MOOT: defect A (GL flipping against a stale cached height) is fixed — engine/render/gl/renderer.cpp:267-276 `Renderer::Impl::drawable_size()` is a guarded `GetClientRect`, read once per frame at :814 in `submit()` and again at :736 in `begin_frame()`, where :743-748 re-stamps every view's default pane from it. There is no cached height left to go stale. WHAT SURVIVES IS THE SHELL **At five backends:** This is the clearest case in the group of a three-way split becoming a five-way one AND of GAPS.md's own stated lesson — "a decomposition by subsystem has a blind spot exactly at the subsystem boundary" (GAPS.md:26-28) — still hol |
| **GE-1** gl-entrypoints (1 of 1): a seam call before create_device is a null FUNCTION POINTER on GL alone, and the 2026 | LANDED | RECONSTRUCTED. The 2026-08-19 dismissal is INVENTORY.md:42: the pre-create_device crash class is "the seam-wide use-before-create_device precondition, which begin_frame hits identically." It does not any more. engine/render/gl/renderer.cpp:734-752 — `begin_frame` calls `drawable_size()` (guarded, :270 `if (this->window == nullptr || !GetClientRect(...))`), then `glViewport`, `glClearColor`, `glClear`, all three of wh **At five backends:** The axis was named `gl-entrypoints` in 2026-08-19 because only GL had a loader; harvest/axes.json renames it `entry-points` and holds all five, which is the right generalisation — d3d11/d3d12 link import libraries (engine/CMakeLis |
| **TH-1** threading (1 of 1): the fan-out runs now, and only against the one backend whose worker path provably touches  | SUPERSEDED | RECONSTRUCTED, and independently re-verified. WHAT LANDED: tests/scene/fanout_tests.cpp and bench/fanout_bench_null.cpp exist, gated one-sidedly — tests/scene/CMakeLists.txt:21-23 `if(LABRADOR_RENDER_BACKEND STREQUAL "null") target_sources(SceneTests PRIVATE fanout_tests.cpp) endif()`, with the reason at :15-20 ("Scene::draw takes a Renderer, and the null backend is the only one with a Renderer that needs no window a **At five backends:** Coverage divides 1/4 rather than the old 1/2. gl and vulkan are unexercised too but would prove nothing new — their worker path is pure memory (vulkan says why at renderer.h:420-423: "a VkCommandPool may not be used from two threa |


## DEFECT A — "GL anchors every pane to a cached back-buffer height; D3D11 needs no height at all" (docs/review/backend-equivalence/README.md:76-107, the prior audit's only LIVE finding), adjudicated at HEAD ca5b2e3 across all five render backends

| Item | Status | What is true at `ca5b2e3` |
|---|---|---|
| **A-a** (a) Is the GL flip still against a cached back-buffer height? — the cache is gone; the flip takes a per-frame  | MOOT | The subject no longer exists. `grep -rn "this->height|impl_->height|Impl::height" engine/render/gl/` returns exactly one hit and it is unrelated — `engine/render/gl/render_resources.cpp:36` (`this->height_`, a font-atlas field). The audit's `Impl::height` is not declared anywhere in the folder. The flip is still one line and still the only place GL converts an origin, at `engine/render/gl/renderer.cpp:595-598`: ```cp **At five backends:** The two backends that landed after the audit (d3d12, vulkan) were both written after the fix, so neither inherited the shape. GL remains the only one of the five that needs a framebuffer height at all — see A-d1/A-d2. |
| **A-b** (b) Is window.cpp unchanged? — yes in every respect the audit cited; the shell still produces the desync twice | NOT_LANDED | `git log --oneline 57b65b3..HEAD -- engine/app/window.cpp` returns exactly one commit, `30a0353 "Gate WM_MOVE the way WM_SIZE is already gated, so a drag means one thing"`, and its diff is 15 added lines + 1 changed line, all inside `case WM_MOVE`. `git log --oneline 4d91b91..HEAD -- engine/app/window.cpp` is **empty** — the file has not been touched since the Vulkan backend landed. None of the three handlers defect  **At five backends:** The audit read this file as producing a *pixel* defect on one backend. At five backends it produces a *policy* difference the seam already declares (renderer.h:432-442): three of the five answer the size they were told and scale,  |
| **A-c** (c) Was a fix applied? — yes, f22c968, and it is wider than the one the audit proposed | LANDED_DIFFERENTLY | `f22c968 "Ask the window how tall it is, which is the one thing GL decides"` (2026-08-19), the commit immediately after `25dcb2b`, the commit that added the audit itself. Five files, +201/-22: `engine/render/gl/backend.h`, `engine/render/gl/renderer.cpp`, `engine/render/renderer.h`, `engine/render/sprite_geometry.h`, `tests/render/pixel_tests.cpp`. The audit proposed one expression: "Have `Impl::replay` take its flip **At five backends:** The fix predates both new backends (f22c968 is 2026-08-19; d3d12 landed 2026-08-20, vulkan 2026-08-21), so it is the state both were written against, not something applied to them retroactively. |
| **A-d1** (d) Does D3D12 have it? — verified, not assumed: no. Its placement is height-free and its one cached viewport  | MOOT | **The placement takes no buffer height.** `to_d3d_viewport` at `engine/render/d3d12/renderer.cpp:44-53` is d3d11's function respelt: ```cpp const RectangleI pixels = viewport.pixel_rect(); return { static_cast<float>(pixels.x), static_cast<float>(pixels.y), static_cast<float>(pixels.width), static_cast<float>(pixels.height), viewport.minDepth, viewport.maxDepth }; ``` `pixels.y` goes straight into `TopLeftY`, absolut **At five backends:** This is the fourth backend, added 2026-08-20 — after the fix. It reproduces d3d11's answer to this axis rather than GL's, which is what the audit's d3d11 column predicted for any Direct3D backend. The prediction is now tested rath |
| **A-d2** (d) Does Vulkan have it? — no, and for a stronger reason than D3D12: the flip is pane-local, so the defect's s | MOOT | **The flip uses only the pane's own rectangle.** `engine/render/vulkan/renderer.cpp:1091-1098`: ```cpp VkViewport pane = {}; pane.x = static_cast<float>(pixels.x); pane.y = static_cast<float>(pixels.y + pixels.height); pane.width = static_cast<float>(pixels.width); pane.height = -static_cast<float>(pixels.height); pane.minDepth = run.viewport.minDepth; pane.maxDepth = run.viewport.maxDepth; vkCmdSetViewport(commands, **At five backends:** This is the term where five backends give three answers, and CLAUDE.md:223-226 already states all three correctly ("Direct3D measures down from the top, GL up from the bottom, and Vulkan hands the rasteriser a negative viewport he |
| **A-d3** (d) null — structurally blind to the axis, unchanged | MOOT | There is no conversion at all in the folder. `DrawList::set_viewport` stores the float `Viewport` on the view (`engine/render/null/renderer.cpp:52-55`) and `View::draw` stamps it verbatim on each `RecordedSprite` (`:27`). No `pixel_rect()` call, no origin term, no sign. `back_buffer_size()` (`:317-321`) returns the two ints `create_device`/`window_size_changed` stored, which is the only size a backend with no framebu **At five backends:** Unchanged from the audit's null column. Under `x64-debug-null` this is also the configuration where `RenderPixelTests` is not built at all, so the seam-level coverage in A-e does not run there. |
| **A-e** (e) Does any golden image or test now cover it? — a seam-level case landed in the fix's own commit; two golden | LANDED_DIFFERENTLY | The audit recorded **"Coverage: none"** and `grep -rn "window_size_changed|back_buffer_size" tests/ bench/ samples/` returning nothing. That is no longer true. Three distinct kinds of coverage now exist, and none of them is a golden image of the defect itself. **1. The staleness condition itself, at the seam.** `tests/render/pixel_tests.cpp:1499` — `TEST_CASE("CONTRACT: read_back_buffer hands back exactly back_buffer **At five backends:** The golden set is one set of 50 images that all four rasterising backends are held to, so the two viewport cases now hold d3d12 and vulkan to GL's and d3d11's flip as well. CI rasterises only the two Direct3D presets (declared in  |
| **A-f** NEW — the sentence defect A's fix amended, engine/render/sprite_geometry.h, is stale at five backends: it coun | NOT_LANDED | `engine/render/sprite_geometry.h:26-27`: *"**Four** backends cannot disagree about where a sprite went, because none of them decides."* There are five backend folders (`ls engine/render/` → d3d11, d3d12, gl, null, vulkan) and five callers of the shared quad builders, the fifth being `engine/render/vulkan/renderer.cpp:212` and `:252`. `engine/render/sprite_geometry.h:29-36`, the paragraph `f22c968` added to declare de **At five backends:** This is the item where five backends changed the answer. At three it was a live behavioural finding; at four it was corrected; at five it is documentation drift only. Note the prior half of this sweep reached the same conclusion i |
| **A-g** NEW — engine/render/renderer.h's back_buffer_size paragraph, the seam text that legislates defect A's term, en | NOT_LANDED | `engine/render/renderer.h:432-442`: *"THE SIZE OF THE BUFFER, NOT OF THE LAST THING ANYONE SAID … What a backend answers then is whatever it is really drawing into: a swap chain does not follow its window, so **both Direct3D backends** answer the size they were told and let Present stretch; a WGL context's default framebuffer is the window's client area, so **the GL backend** answers the window."* Every clause is tru **At five backends:** This paragraph is the seam's specification of the exact quantity defect A was about. It was last re-counted for the fourth backend and never for the fifth. If any one line in engine/render/ should be current at five backends, it i |
| **A-h** NEW — the shell's restore-from-minimise hole now yields a different wrong picture per backend, because Applica | NOT_LANDED | This is an asymmetry the fix created and that nothing in the tree names outside a commit message. **The loop.** `engine/app/application.cpp:380-385`: ```cpp void Application::on_window_moved() const { const mattmath::Vector2F size = this->renderer_->back_buffer_size(); std::ignore = this->renderer_->window_size_changed( static_cast<int>(size.x), static_cast<int>(size.y)); } ``` It calls `window_size_changed` and **no **At five backends:** At three backends this loop was harmless because GL's back_buffer_size was itself cached, so all three early-outed. Making GL's answer live — the fix — is what created the fork. d3d12 and vulkan both joined the majority side, so t |


## Defect B (docs/review/backend-equivalence/README.md §B) — begin_frame and the frame begun but never submitted, re-adjudicated at HEAD ca5b2e3 against five backends

| Item | Status | What is true at `ca5b2e3` |
|---|---|---|
| **B-a** (a) Is the D3D11 behaviour unchanged? — No. The undrainable deferred context no longer exists. | MOOT | The defect's mechanism is gone. `View::reset()` is still exactly what the audit described — engine/render/d3d11/renderer.cpp:224-233 clears the CPU staging batch and ends `this->bound = false;` — but it is no longer the only thing begin_frame does to a view. engine/render/d3d11/renderer.cpp:818-822: for (std::unique_ptr<DrawList::View>& view : this->impl_->views) { view->discard(); view->reset(); } `DrawList::View::d **At five backends:** The three-way split the audit found (gl/null drop it, d3d11 defers it) is now a five-way agreement: all five drop it, by five different mechanisms. High confidence. |
| **B-b** (b) Was a fix applied? — Yes, in commit cc02678, and it amended the seam and both harnesses in the same commit | LANDED | `git log -S "void DrawList::View::discard" -- engine/render/d3d11/renderer.cpp` returns exactly one commit: cc02678 "Drain the frame that was abandoned, which one backend kept" (2026-08-19, i.e. the day the audit was written). It touches five files: engine/render/d3d11/backend.h (+19), engine/render/d3d11/renderer.cpp (+51/-6), engine/render/renderer.h (+11), tests/render/null_tests.cpp (+30), tests/render/pixel_test **At five backends:** cc02678 predates d3d12 (2026-08-20) and vulkan (2026-08-21) by one and two days, so the fix is the *older* code here — both later backends were written to a seam that already stated the rule, and each cites it by name in its own b |
| **B-c-d3d12** (c) NEW: what does D3D12 do with a frame begun and never submitted? — Closes the lists without executing them; | LANDED | `Renderer::begin_frame` (engine/render/d3d12/renderer.cpp:1118-1150) runs in this order: `impl.device_resources.wait_for_frame();` (:1126) → `impl.abandon_recording();` (:1136) → `impl.view_count = 0;` (:1141) → `impl.frame_begun = true;` (:1147) → `impl.open_frame();` (:1149). `Renderer::Impl::abandon_recording` (engine/render/d3d12/renderer.cpp:506-529) opens with the answer: "CLOSED, NOT EXECUTED, AND THAT IS THE  **At five backends:** D3D12 is the backend whose begin_frame comment already enumerates the five: engine/render/d3d12/renderer.cpp:1130-1132, "The five backends have three different things to drop: three clear a vector, the D3D11 one drains a deferred  |
| **B-c-vulkan** (c) NEW: what does Vulkan do with a frame begun and never submitted? — Resets the command pool and the descrip | LANDED | `Renderer::begin_frame` (engine/render/vulkan/renderer.cpp:870-898) is D3D12's order line for line: `impl.device_resources.wait_for_frame();` (:879, comment at :874-878 — "it is the same line the D3D12 one adds, because the two APIs ask the same thing of the engine") → `impl.abandon_recording();` (:884) → `view_count = 0` → `frame_begun = true` → `open_frame()`. `Renderer::Impl::abandon_recording` (engine/render/vulk **At five backends:** Vulkan is a *fourth* kind of reset, not one of the three the seam enumerates: a vector per view plus a command pool plus descriptor pools plus discarded layout tracking. engine/render/vulkan/renderer.cpp:825-841 also records the o |
| **B-c-gl-null** (c) The other two, re-read at HEAD: gl and null are unchanged and still trivially correct. | LANDED | gl — `Renderer::begin_frame` (engine/render/gl/renderer.cpp:734-752) does `glViewport` / `glClearColor` / `glClear(GL_COLOR_BUFFER_BIT)` and then `view->reset()` over every view plus a full-drawable pane; `View::reset()` (engine/render/gl/renderer.cpp:141-153) is `vertices.clear(); runs.clear();`. `end_frame` is `SwapBuffers` alone (:754-757). A frame begun and never submitted leaves nothing. null — `Renderer::begin_ **At five backends:** gl, vulkan and null are the three that hold a frame in a vector. That is the count the seam paragraph gets wrong; see B-residual-drift. |
| **B-d** (d) Reachability of the worker-exception route, re-assessed — the audit's specific route is still present verb | LANDED | All three legs verified at HEAD. Leg 1, the throw site, unchanged: `Renderer::Impl::texture_size` (engine/render/d3d11/renderer.cpp:407-424) still contains `ThrowIfFailed(resource.As(&texture_2d));`, under the comment "ASKED OF THE VIEW, EVERY DRAW". Its two callers are both per-draw and both on worker threads: engine/render/d3d11/renderer.cpp:334 (inside `DrawList::draw_sprite`) and :364 (inside `DrawList::draw_text **At five backends:** The route is D3D11-only in its specific form (only that backend calls into the runtime per sprite from a worker), but the general form — any throw out of a client's `draw()` under the fan-out — reaches all five, and all five now h |
| **B-e** (e) Does any test now pair a begin with no submit? — Yes, three, and the pixel one is byte-pinned by a golden  | LANDED | The audit's "Coverage: none. Both harnesses always pair begin with submit" (docs/review/backend-equivalence/README.md:123) is false at HEAD. 1. tests/render/pixel_tests.cpp:1467, `TEST_CASE("CONTRACT: a frame that is never submitted contributes nothing")`. It calls `harness.begin()` (which is `begin_frame()` + `set_view_count()`, pixel_tests.cpp:288-293), draws a sprite *and then* text — the comment at :1469-1473 say **At five backends:** This is where the term became five-backend without anybody restating it: cc02678 wrote both cases when there were three backends, and d3d12 and vulkan inherited the assertion by being built into RenderPixelTests. Vulkan's synchron |
| **B-residual-drift** THE ONE THING STILL OPEN: the seam paragraph that specifies defect B's rule counts four of the five backends,  | NOT_LANDED | engine/render/renderer.h:367-379, the `begin_frame` paragraph and the only place this rule is written down: // A FRAME BEGUN AND NEVER SUBMITTED CONTRIBUTES NOTHING TO THE NEXT // ONE, which is a statement about what "resets" means and is worth // making because the five backends have three different things to // reset. Two of them hold a frame in a vector, where dropping it is // clearing the vector; the D3D11 one h **At five backends:** This is the same shape as the `dropped-views` survivor from the first half (engine/render/renderer.h:391, "on two backends it is a vector nothing will replay… and on the D3D12 one a closed command list", which says "All five throw |


## DEFECT C — add_texture_asset before create_device (backend-equivalence audit, 2026-08-19, 57b65b3) and its TEST-GAP vehicle A3

| Item | Status | What is true at `ca5b2e3` |
|---|---|---|
| **C-a** The three behaviours do NOT stand — all five backends now refuse a pre-create_device load with std::runtime_er | LANDED | Fixed by commit 1753b73 "Answer a load before the device the same way three times" (2026-08-19 21:02, i.e. the same day the audit was written), which touched engine/render/d3d11/texture_factory.cpp, engine/render/null/backend.h, engine/render/null/renderer.cpp, engine/render/null/texture_factory.cpp, engine/render/resource_factory.h, tests/render/CMakeLists.txt and the new tests/render/renderer_seam_tests.cpp. All fi **At five backends:** This went from a three-way split to a five-way agreement rather than to a five-way split. d3d12 (2026-08-20) and vulkan (2026-08-21) each arrived carrying the guard already, written to the seam rule 1753b73 had put in resource_fac |
| **C-b** The create_device-before-load rule was written onto the seam, but onto resource_factory.h, not renderer.h | LANDED_DIFFERENTLY | GREP OF renderer.h IS EMPTY FOR THE RULE. `grep -n "create_device" engine/render/renderer.h` returns exactly four lines and none of them is about loading: :275 (the declaration `void create_device(void* native_window, int width, int height, int view_capacity);`), :353 ("Separate from create_device because the order is fixed the other way"), :384 ("Throws std::out_of_range above the capacity create_device was given"), **At five backends:** The seam paragraph's historical clause — "the three backends answered it three ways" (resource_factory.h:82-84) — is PAST TENSE and describes 2026-08-19 accurately, so it is not stale prose despite containing the number three. Sam |
| **C-c-d3d12** The fourth answer: d3d12 refuses, and one device check covers everything else it has to be null | LANDED | engine/render/d3d12/texture_factory.cpp:105-114, the first statement of the function (signature at :102): ``` // BEFORE ANYTHING ELSE, because the ordering is a rule of the seam // (resource_factory.h) and the honest failure is a named throw rather // than a null dereference inside the runtime. if (device_of(renderer) == nullptr) { throw std::runtime_error("Texture '" + name + "' was loaded before " "create_device ma **At five backends:** d3d12's answer is byte-identical to d3d11's and vulkan's, message included — the three that hold a device pointer share one sentence. This axis therefore did NOT go from three ways to five ways; it went from three ways to one way, |
| **C-c-vulkan** The fifth answer: vulkan refuses, and its device() accessor is the null-safe one while owner() next to it is n | LANDED | engine/render/vulkan/texture_factory.cpp:135-142, the first statement of the function (signature at :130): ``` // BEFORE ANYTHING ELSE, because the ordering is a rule of the seam // (resource_factory.h) and the honest failure is a named throw rather // than a null dereference inside the loader. if (device_of(renderer) == nullptr) { throw std::runtime_error("Texture '" + name + "' was loaded before " "create_device ma **At five backends:** Five backends, five guards, one rule, and two distinct null-detection mechanisms across them: three read a device/context pointer (d3d11 `GetD3DDevice()`, d3d12 `device_.Get()`, vulkan `owner_ ? owner_->device : VK_NULL_HANDLE`),  |
| **C-d-A3** TEST-GAP A3 landed, in exactly the vehicle it proposed, and it is the only case in the file | LANDED | tests/render/renderer_seam_tests.cpp is 97 lines and contains exactly one TEST_CASE, at :51: `TEST_CASE("CONTRACT: a texture loaded before there is a device is refused, by name")` — TEST-GAP.md:28's A3 is `CONTRACT: loading a texture before there is a device is refused, by name`. It is listed unconditionally at tests/render/CMakeLists.txt:10, inside the `add_executable(RenderTests ...)` list at :3-19, above and outsi **At five backends:** This is the only file in tests/ that holds all five backends to one statement, and it holds them to exactly one statement. RenderPixelTests is not built against null (tests/render/CMakeLists.txt:66), null_tests.cpp compiles only a |
| **C-d-A1245** The A-vehicle carries one of its five proposed cases: A1, A2, A4 and A5 did not land anywhere | NOT_LANDED | tests/render/renderer_seam_tests.cpp has exactly one TEST_CASE (`grep -n "TEST_CASE|SUBCASE" tests/render/renderer_seam_tests.cpp` returns the single line 51). The other four proposals in TEST-GAP.md:26-30 are absent: - A1 (`create_device(nullptr, 64, 64, 0)` and `(..., -1)` throw std::invalid_argument; `set_view_count(-1)` throws std::out_of_range). `grep -rn "invalid_argument" tests/` returns hits in tests/app/appl **At five backends:** Every one of A1, A4 and A5 is now a five-way question rather than the three-way one the proposal wrote, and A1 in particular is already unanimous in the code (the `view_capacity < 1` throw is the first statement of create_device o |
| **C-e** Three of the five guard comments still count three or four backends — corroborating, not new | LANDED_DIFFERENTLY | The code is right in all five places (item C-a); the prose around it is not, in three of them. All quoted verbatim at HEAD: - engine/render/d3d11/texture_factory.cpp:78-79: "The seam states the ordering (resource_factory.h); this is one of the three places it is kept." It is one of FIVE (d3d11:80, d3d12:110, gl:90, vulkan:138, null:39). - engine/render/null/texture_factory.cpp:31-33: "The other two backends refuse th **At five backends:** This is the group's contribution to the sweep's live question — does engine/render/ still describe itself accurately at five backends. On this rule the answer is: the CODE does, unanimously and by name in all five backends; the SE |


## survey 2026-08-26 §6 — the two render decisions (reference machine; markers on the seam), adjudicated at HEAD ca5b2e3

| Item | Status | What is true at `ca5b2e3` |
|---|---|---|
| **R1** THE REFERENCE MACHINE — the decision was made, and it is in PHILOSOPHY.md, Performance | LANDED | `docs/design/PHILOSOPHY.md:623-636`, landed by `c8176ef` ("Name the low tier, and say which half of it is measured"): "**\"The low tier\" is a named configuration, and this is it.** Four backend comments size real decisions against that phrase, and it is defined here because it is a target and not a report ... It means the Radeon Graphics adapter integrated into the Ryzen 9000 desktop package (PCI `1002:13C0`) — two  **At five backends:** The definition is load-bearing for four of the five backends and says so: "all four rasterising backends run on it the way they ship" (`PHILOSOPHY.md:634`). `render/null/` is outside it by construction — it has no adapter. This cl |
| **R2** WHICH HALF IS MEASURED: the GPU half is named, the CPU half is neither named nor measured — and no p99 exists  | LANDED_DIFFERENTLY | The split is written in two places. `docs/design/PHILOSOPHY.md:637-645`: "Holding the process to four cores changes how many workers the fan-out gets, not how fast one of them is ... The four claims above do not all need the same evidence: a 256 KB vertex page is a memory budget and is checkable against a written spec, while the two about pipeline depth are latency and need a measured p99. Until that exists no number **At five backends:** The unmeasured half falls entirely on the two newest backends — the pipeline-depth claim exists only in d3d12/ and vulkan/, which did not exist when the term was coined. Going from three backends to five is what turned one memory- |
| **R3** SITE 1 of 5 — engine/render/d3d11/backend.h:105-112 (MAX_BATCH_SPRITES): has a referent AND a number | LANDED | `engine/render/d3d11/backend.h:105-112` at HEAD: "// How many sprites one vertex buffer holds. DirectXTK's number, and / // arrived at the same way: 16-bit indices cap it at 16384 sprites, and / // a buffer of 2048 is 256KB of vertices per view - which is affordable / // on the low tier and large enough that the fill-and-flush path is not / // what a frame is spending its time on. PHILOSOPHY.md, Performance, / // nam **At five backends:** The 2048 constant is now replicated in four of five backends (`d3d11/backend.h:112` MAX_BATCH_SPRITES, `d3d12/backend.h:153` MAX_PAGE_SPRITES, `gl/backend.h:118` MAX_RUN_SPRITES, `vulkan/backend.h:148` MAX_RUN_SPRITES) with three  |
| **R4** SITE 2 of 5 — engine/render/d3d12/backend.h:147-153: referent is one hop away, number present; the comment its | LANDED_DIFFERENTLY | `engine/render/d3d12/backend.h:147-153` at HEAD, UNCHANGED by `c8176ef` (`git show --stat c8176ef` lists only PHILOSOPHY.md, next-status.md, d3d11/backend.h, d3d12/device_resources.h, linesweeper/README.md): "// How many sprites one vertex page holds. The same 2048 the D3D11 / // backend uses, arrived at the same way: 16-bit indices cap a page at / // 16384 sprites, and 2048 of them is 256KB - affordable on the low t **At five backends:** The deference chain only exists because d3d12/ was written after d3d11/. It is sound. The chain's other two ends are NOT low-tier sites at all — see R8. |
| **R5** SITE 3 of 5 — engine/render/d3d12/device_resources.h:53-69 (FRAME_COUNT = 2): referent present, claim classifi | LANDED | `engine/render/d3d12/device_resources.h:57-69`: "// TWO, NOT THREE. Three is the usual answer for a title that wants the / // GPU never to starve; two is the answer for a 2D sprite engine sized / // for the low tier, where a frame is tens of draw calls and the per- / // frame cost of a third set - a command allocator and a vertex page / // per view - buys latency nobody asked for." then, added by `c8176ef` at `:66-68 **At five backends:** This site did not exist before 2026-08-20. It is the head of the second chain, which vulkan/device_resources.h defers to (R6) — a two-site chain that is entirely a consequence of the fourth and fifth backends. |
| **R6** SITE 4 of 5 — engine/render/vulkan/device_resources.h:187-194: referent two hops away, no number, and it inher | LANDED_DIFFERENTLY | `engine/render/vulkan/device_resources.h:187-194` at HEAD, UNCHANGED by `c8176ef`: "// HOW MANY FRAMES THE CPU MAY RUN AHEAD. Two, for the reason / // engine/render/d3d12/device_resources.h gives: three is the answer for / // a title that wants the GPU never to starve, two is the answer for a / // 2D sprite engine sized for the low tier, where the per-frame cost of / // a third set is a command pool, a vertex buffer  **At five backends:** This site is nine days old at HEAD. Both pipeline-depth sites (R5, R6) are new-backend sites, and both are on the unmeasured side of the split — so the two newest backends contributed 100% of the claims still waiting on hardware. |
| **R7** SITE 5 of 5 — engine/render/d3d12/device_resources.cpp:20-30: the site the survey counted but never named, nev | NOT_LANDED | GREP: `grep -rni "low tier|low-tier" engine/` returns six lines across five distinct comment sites; the fifth is `engine/render/d3d12/device_resources.cpp:29`. Full block at `:20-31`: "// THE FLOOR, AND IT IS HIGHER THAN THE ENGINE'S. // ... engine/render/d3d11/device_resources.h puts this engine's floor at / // feature level 10.0. Direct3D 12 has no such level: 11_0 is the lowest / // there is, and the OS floor is W **At five backends:** Purely a d3d12 site; vulkan's own floor discussion does not use the phrase. It is the only NEGATIVE low-tier claim in the tree — the other four say "affordable on", this one says "not a target for" — which is why it reads as noise |
| **R8** The other two ends of the 2048 chain are NOT low-tier sites, which is why the count is five and not seven | MOOT | `engine/render/gl/backend.h:115-118`: "// How many sprites one run may hold. The index buffer is built once for / // this many and reused with a base vertex, so a longer run is split / // rather than growing anything." — a mechanical statement, no cost claim, no referent needed. `engine/render/vulkan/backend.h:143-148`: "// ... The same 2048 the other three drawing / // backends use, arrived at the same way: 16-bit i **At five backends:** This is the five-backend correction to the survey's own framing: the 2048 constant spread to four backends but the *claim about hardware* did not spread with it. Two backends copied the number and dropped the assertion, which is t |
| **R9** samples/linesweeper/README.md contradicts itself about the reference machine, 18 lines apart, at HEAD | NOT_LANDED | `samples/linesweeper/README.md:446-448`: "It remains a prediction about arithmetic rather than a frame time, and the / **reference machine is still unnamed**, so no number here is a floor. See *Still / open*." Against `:466-470`, which `c8176ef` rewrote: "- **The reference machine — named now, and still unmeasured.** ... Two of the three have landed: `PHILOSOPHY.md`, Performance names the configuration, and the resol **At five backends:** Not backend-specific. Included because it is the same failure mode as R7 and from the same commit: an amendment that landed where the claim was made and not where the claim was cited. |
| **R10** CLAUDE.md is not a low-tier site and never was — nothing was needed there | MOOT | GREP, empty: `grep -n -i "low tier|low-tier|lowest tier|reference machine|modest|constrained" CLAUDE.md` returns nothing. The only two hits for `hardware` are `CLAUDE.md:90` (the golden-image channel-drift allowance, "a hardware adapter and the WARP one CI has") and `CLAUDE.md:310` ("runs on hardware this repository already has", about the Android port's spine) — neither sizes a decision. Tree-wide, `grep -rn -i "ref **At five backends:** n/a. |
| **R11** The successor decision is already filed: the 2013 MacBook Air, and it strands exactly the two unmeasured claim | LANDED | `docs/survey/2026-08-26-status.md:519-527`: "A 2013 MacBook Air is the one genuinely low-tier *whole* machine in reach — a dual-core Haswell fixes exactly the CPU gap above. Under Boot Camp it runs D3D11 (HD 5000 is feature level 11_0 against a 10_0 floor) and GL 3.3 core (Intel's Haswell driver exposes 4.3), and it runs neither D3D12 nor Vulkan, because Intel's Windows drivers for both start at Skylake and Haswell h **At five backends:** This is a five-backend consequence with no three-backend analogue: at three backends (d3d11/gl/null) the Air would have covered the whole rasterising surface. d3d12/ and vulkan/ are precisely what it cannot reach. |
| **M1** WHETHER MARKERS STAY ON THE SEAM — the decision was not made, and neither branch of it was taken | NOT_LANDED | Survey §6 (`docs/survey/2026-08-26.md:331-335`) put it as a binary: "Either the seam says markers are advisory and may do nothing, or the three methods come off and `renderer.h:490-502` loses the paragraph claiming they reach the seam unchanged." `docs/survey/2026-08-26-status.md:530` records the outcome in four words: "2. **Whether markers stay on the seam.** Untouched by any of this." Verified at HEAD: the paragrap **At five backends:** The survey wrote "four of five backends discard it" and that is exact at HEAD. At the 2026-08-19 audit it was two of three. The ratio moved against the seam, not for it. |
| **M2** The five-backend state of the axis, re-verified at HEAD: one forwards, four discard, and all four argue in pla | LANDED_DIFFERENTLY | d3d11 is the only forwarder — `engine/render/d3d11/renderer.cpp:1005-1018` calls `this->impl_->device_resources.PIXBeginEvent(name);` into `engine/render/d3d11/device_resources.h:92-105`, which calls `m_d3dAnnotation->BeginEvent(name)`. The other four are `std::ignore = name;` and touch no member: `d3d12/renderer.cpp:1363-1375` (argued `:1357-1362`, T9: the event blob's encoding is WinPixEventRuntime's and "this repo **At five backends:** This is the strongest five-backend fact in the item. At three backends the split was 1:2 and readable as 'GL is the odd one out' — which is what gl/renderer.cpp:875 still says. At five it is 1:4 and d3d11 is the outlier. |
| **M3** RECOMMENDATION — keep the three methods; amend renderer.h:490-502 to declare markers advisory. T6, PHILOSOPHY. | NOT_LANDED | T6 is the trade-off that decides this and it decides it against deletion. `PHILOSOPHY.md:108-111`: "When the engine can't do what was asked, it says so immediately ... Quiet failure is the most expensive kind, and doubly so for a user who didn't write the engine." But `PHILOSOPHY.md:116-119`, T6's own carve-out: "**Not a licence for:** throwing on the way out ... nor for treating expected absence as failure. A discon **At five backends:** The two new backends strengthen the keep side rather than the delete side, and this is the part the 2026-08-19 audit could not see. PIX and VK_EXT_debug_utils are both real facilities that exist in the box; d3d12's no-op is a T9 p |
| **M4** The amendment has to answer TEST-GAP A2 in the same commit — neither half of that 2026-08-19 either/or was eve | NOT_LANDED | `docs/review/backend-equivalence/TEST-GAP.md:27`: "**A2** `CONTRACT: a marker is legal before there is a device` — **fails on d3d11 today** (`device_resources.h:92-105` derefs `m_d3dAnnotation` unguarded). **Either the seam states markers need a device or d3d11 gains three lines.**" Neither happened. The seam still states no precondition (`renderer.h:490-502`, M1). And d3d11 still has no guard: `engine/render/d3d11/d **At five backends:** Four of five backends already pass A2 for free, because their bodies read no member at all (`d3d12:1365`, `gl:878`, `vulkan:1373`, `null:340`). A2 is a one-backend fix that turns an accident into a guarantee. |
| **M5** Zero marker coverage in all five configurations, and the vehicle for it exists and holds exactly one case | NOT_LANDED | GREP, empty: `grep -rn -i "marker" tests/ bench/` returns nothing (exit 1). `tests/render/renderer_seam_tests.cpp` — the file TEST-GAP proposed as Vehicle A and the one CLAUDE.md describes as "everything the seam answers without a device" — is 97 lines and holds ONE `TEST_CASE`, at `:51`: "CONTRACT: a texture loaded before there is a device is refused, by name" (that is A3). A1, A2, A4 and A5 were never written. Its  **At five backends:** This is the axis's real gap and it is five-wide, not per-backend. `RenderPixelTests` cannot see a marker (no pixel), `null_tests.cpp` cannot (no field), and `renderer_seam_tests.cpp` does not. The one configuration built to record |
| **M6** gl/renderer.cpp:875 still says 'the other backend' — written at two backends, false at five | NOT_LANDED | `engine/render/gl/renderer.cpp:869-875` at HEAD: "// Debug markers, and this backend has none. // // KHR_debug's glPushDebugGroup is the equivalent and is not wired up: it is / // an extension, it is a debugging convenience rather than anything a frame / // depends on, and a marker that does nothing is honest where a marker that / // silently needs an extension is not. **The seam's three calls exist because / // the  **At five backends:** This is the marker axis's contribution to the sweep's headline. It is the only stale COUNT on the axis — d3d12, vulkan and null all state their position without counting anyone. |
| **M7** vulkan/renderer.cpp's marker argument reaches the right conclusion from an imprecise premise, and omits the de | LANDED_DIFFERENTLY | `engine/render/vulkan/renderer.cpp:1363-1367`: "a label belongs to a COMMAND BUFFER, and these three may be called at any point a client likes, including when no frame is open and nothing is recording. D3D11 can answer because ID3DUserDefinedAnnotation hangs off the device context and is always there; **this API has nothing at that level**". VK_EXT_debug_utils does have something at that level: `vkQueueBeginDebugUtil **At five backends:** It is the fifth backend's comment and the only one of the five that argues from an API capability it has rather than one it lacks — which is why it is the one worth getting exactly right. |
| **M8** Two riders on the marker surface: set_marker has zero callers tree-wide, and the audit that noted it cites a s | LANDED_DIFFERENTLY | GREP: `grep -rn "begin_marker|end_marker|set_marker" --include=*.cpp --include=*.h .` outside `engine/render/` returns exactly two code lines — `engine/app/application.cpp:293` (`this->renderer_->begin_marker(L"Render");`) and `:296` (`this->renderer_->end_marker();`), wrapping `StateContext::draw` at `:294` and `submit()` at `:295`, inside `begin_frame()` at `:291` / `end_frame()` at `:298`. `set_marker` has no call **At five backends:** The caller is unchanged in shape from the three-backend era and is inside the engine, not a sample: `application.cpp` annotates the whole frame on every backend, which means four of five builds pay a virtual-free but non-inlined s |

